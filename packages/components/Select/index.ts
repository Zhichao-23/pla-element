import { withInstall } from '@pla-element/utils';
import Select from './Select.vue';
import Option from './Option.vue';
export const PlaSelect = withInstall(Select);
export const PlaOption = withInstall(Option);